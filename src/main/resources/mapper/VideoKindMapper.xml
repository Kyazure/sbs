<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="online.kyralo.video.dao.VideoKindMapper">
  <resultMap id="videoKind" type="online.kyralo.video.domain.VideoKind">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="name" jdbcType="CHAR" property="name" />
    <result column="info" jdbcType="VARCHAR" property="info" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
  </resultMap>

  <select id="queryVideoKindById" resultMap="videoKind">
    select id, name, info, create_time, update_time
    from video_kind
    where id = #{id}
  </select>

  <select id="listVideoKinds" resultMap="videoKind">
      select id, name, info, create_time, update_time
      from video_kind
      where 1 = 1
  </select>

  <insert id="insert">
    insert into video_kind(name, info)
    values (#{name}, #{info})
  </insert>

</mapper>